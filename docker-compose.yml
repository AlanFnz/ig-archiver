services:
  ig-archiver:
    build:
      context: ./server
    ports:
      - "127.0.0.1:3000:3000"
    env_file:
      - ./server/.env
    environment:
      DATA_DIR: /data
      HOST: 0.0.0.0
    volumes:
      - ./server/data:/data
    restart: unless-stopped
